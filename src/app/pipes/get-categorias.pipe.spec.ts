import { GetCategoriasPipe } from './get-categorias.pipe';

describe('GetCategoriasPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCategoriasPipe();
    expect(pipe).toBeTruthy();
  });
});
